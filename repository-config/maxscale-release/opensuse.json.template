[
{
   "product":		"maxscale",
   "version":		"default",
   "repo":		"${production_repo_prefix}/$version/opensuse/${platform_version}/\$basearch",
   "repo_key":		"${rpm_repo_key}",
   "platform":		"opensuse",
   "platform_version":	"${platform_version}"
}
}
]
