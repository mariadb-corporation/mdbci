[
{
   "product":		"maxscale",
   "version":		"default",
   "repo":		"${production_repo_prefix}/$version/sles/${platform_version}/\$basearch",
   "repo_key":		"${rpm_repo_key}",
   "platform":		"sles",
   "platform_version":	"${platform_version}"
}
}
]
